import { Autocomplete, AutocompleteItem } from "@heroui/react";
import { useState } from "react";
import { useLoaderData, useFetcher } from "react-router";



export function NewFoodFilter({} : {}) {
    const foodsList = useLoaderData();    

    return
}
